<nav id="navBar" class="navbar navbar-expand-sm navbar-light bg-light fixed-top mb-2">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      {# <div class="navbar-nav"> #}
        <div class="navbar-nav d-flex justify-content-between container">
          <div class="d-flex justify-content-evenly">
            <a class="nav-link buttonHovered mx-3" href="{{ path('default') }}">Home</a>
            <a class="nav-link buttonHovered mx-3" href="{{ path('recettes') }}">Recettes</a>
            {% if app.user %}
              <a class="nav-link buttonHovered mx-3" href="{{ path('app_course_index') }}">Liste de course</a>
            {% endif %}  
          </div>
          <div>
            {% if app.user %}
              <a class="buttonHovered mx-3" href="{{ path('app_logout') }}"><i class="bi bi-person-x-fill text-danger" style="font-size: 30px"></i></a>
              {% else %}
              <a class="nav-link buttonHovered mx-3" href="{{ path('app_register') }}">S'enregistrer</a>
              <a class="buttonHovered" href="{{ path('app_login') }}"><i class="bi bi-person" style="font-size: 30px"></i></a>
            {% endif %}          
          </div>
        </div>
        
        {# <a class="nav-link" aria-current="page" href="{{ path('default') }}">Home</a>
        <a class="nav-link" href="{{ path('recettes') }}">Recettes</a>
        {% if app.user %}
          <a class="nav-link" href="{{ path('app_course_index') }}">Liste de course</a>
          <a class="" href="{{ path('app_logout') }}"><i class="bi bi-person-x-fill text-danger" style="font-size: 30px"></i></a>
        {% else %}
          <a class="nav-link" href="{{ path('app_register') }}">Register</a>
          <a class="nav-link" href="{{ path('app_login') }}"><i class="bi bi-person" style="font-size: 30px"></i></a>
        {% endif %} #}
      {# </div> #}
    </div>
  </div>
</nav>
