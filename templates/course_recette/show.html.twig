{% extends 'base.html.twig' %}

{% block title %}CourseRecette{% endblock %}

{% block body %}
    <h1>CourseRecette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course_recette.id }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ course_recette.qty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_course_recette_index') }}">back to list</a>

    <a href="{{ path('app_course_recette_edit', {'id': course_recette.id}) }}">edit</a>

    {{ include('course_recette/_delete_form.html.twig') }}
{% endblock %}
