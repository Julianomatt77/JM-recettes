{% extends 'base.html.twig' %}

{% block title %}IngredientPerRecette{% endblock %}

{% block body %}
    <h1>IngredientPerRecette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredient_per_recette.id }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ ingredient_per_recette.qty }}</td>
            </tr>
            <tr>
                <th>Qty_pp</th>
                <td>{{ ingredient_per_recette.qtyPp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_per_recette_index') }}">back to list</a>

    <a href="{{ path('app_ingredient_per_recette_edit', {'id': ingredient_per_recette.id}) }}">edit</a>

    {{ include('ingredient_per_recette/_delete_form.html.twig') }}
{% endblock %}
