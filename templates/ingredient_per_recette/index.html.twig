{% extends 'base.html.twig' %}

{% block title %}IngredientPerRecette index{% endblock %}

{% block body %}
    <h1>IngredientPerRecette index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Qty</th>
                <th>Qty_pp</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ingredient_per_recette in ingredient_per_recettes %}
            <tr>
                <td>{{ ingredient_per_recette.id }}</td>
                <td>{{ ingredient_per_recette.qty }}</td>
                <td>{{ ingredient_per_recette.qtyPp }}</td>
                <td>
                    <a href="{{ path('app_ingredient_per_recette_show', {'id': ingredient_per_recette.id}) }}">show</a>
                    <a href="{{ path('app_ingredient_per_recette_edit', {'id': ingredient_per_recette.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_per_recette_new') }}">Create new</a>
{% endblock %}
